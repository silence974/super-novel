# RELIABILITY.md

## 目的

定义本地数据可靠性、项目快照、回滚、迁移、索引重建和检查失败恢复策略。

## 可靠性目标

- 单人本地使用，不追求企业级容灾。
- 数据库由应用托管，用户不感知底层服务。
- 作品内容和设定变更必须可恢复。
- 索引损坏不应导致事实丢失。
- 增量检查失败时应能回退到全量检查。

## 数据分层可靠性

### 事实源数据

包括：

- 作品内容。
- 已确认设定。
- 已确认事件。
- 用户配置的历法和时间域。
- 版本快照。

事实源必须优先保证一致性和可备份。

### 派生数据

包括：

- 向量索引。
- 全文索引。
- 图查询缓存。
- 检查结果索引。

派生数据必须可重建。

## 项目快照

优先目标：支持整个项目状态的大版本回滚。

最低目标：

- 支持作品内容回滚。
- 支持设定恢复。
- 支持回滚后重新构建索引。

快照必须包含：

- 快照 ID。
- 创建时间。
- 用户说明。
- 作品内容版本。
- 设定版本。
- 事件版本。
- 数据库 schema 版本。

## 当前 Spike 结论

`spikes/tauri-minimal` 已验证最低成本快照恢复闭环：

1. 创建或打开本地 SQLite 项目数据库。
2. 将数据库文件复制到 `snapshots/` 目录。
3. 故意破坏章节标题和事实数据。
4. 用快照文件覆盖项目数据库。
5. 重新打开数据库后，章节标题和冲突数量恢复到快照状态。

当前结论：

- 单 SQLite 数据库文件可以使用文件复制作为首期项目快照方案。
- 恢复前必须确保数据库连接释放，或使用 SQLite backup API 避免热拷贝风险。
- 多数据库组合尚未验证一致性快照，不能直接套用单文件策略。
- 正式产品需要在快照元数据中记录 schema 版本、创建时间、用户说明和索引重建状态。

## 数据库迁移

迁移必须满足：

- 每次 schema 变更有迁移版本。
- 迁移前自动创建快照。
- 迁移失败可恢复到迁移前状态。
- `docs/generated/db-schema.md` 同步更新。

## 增量检查可靠性

增量检查必须记录：

- 触发来源。
- 影响实体。
- 影响章节。
- 影响事件。
- 检查规则版本。

如果影响范围计算失败，必须提示用户执行全量检查。

## 崩溃恢复

应用启动时应检查：

- 是否存在未完成写入事务。
- 是否存在未完成补丁应用。
- 是否存在损坏索引。
- 是否需要重建检查结果。

## Open Questions

- 单 SQLite 项目快照已验证文件复制恢复可行；正式产品仍需确认是否使用 SQLite backup API、事务边界和快照元数据策略。
- 图数据库或向量库如果独立存储，多数据库快照一致性策略需要单独验证。
