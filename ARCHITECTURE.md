# ARCHITECTURE.md

## 目的

定义 `super-novel` 的系统架构、模块边界、数据流和首期技术路线。

## 架构图

生成图位于 `docs/generated/architecture-diagram.png`。

文字版架构以本文为准。如果图片文字或模块边界与本文不一致，以本文为事实源。

## 总体分层

```text
Windows 桌面端 / 作品控制台 / 分栏工作台
  -> Agent 编排层（人工触发）
  -> 核心领域服务
  -> 本地数据层
  -> OpenAI API Adapter
```

## 桌面端层

职责：

- 提供 Windows 原生体验优先的桌面应用。
- 展示作品控制台。
- 提供章节树、正文编辑区、设定侧栏、事件侧栏、检查面板和 AI 操作面板。
- 将用户操作转换为领域服务命令。

推荐技术：

- 首选：`Tauri v2 + Rust backend + WebView2`。
- 备选：`.NET/WinUI`。
- 不优先选择：`Electron`，原因是安装体积和资源占用不符合当前优先级。

Assumption: Tauri 打包体积、WebView2 分发策略和数据库绑定需要通过 spike 验证。

## Agent 编排层

Agent 编排层不是自动驾驶系统，而是人工触发的工具编排层。

职责：

- 大纲生成。
- 分章大纲生成。
- 正文生成、润色、重写。
- 从正文或大纲中抽取候选事实。
- 发起手动检查或增量检查。
- 根据冲突生成修复补丁。
- 将 AI 输出标记为候选，等待用户确认。

约束：

- 不允许 AI 直接写入正式事实库。
- 不允许自动跨阶段连续执行不可见操作。
- 每一步必须有用户可理解的输入、输出和确认动作。

## 核心领域服务层

### 强约束时空状态机

负责事件级、时间戳级的状态推导和约束检查。

支持：

- 架空历法。
- 多时间域。
- 不同时间流速。
- 倒叙的叙事顺序与世界内时间分离。
- 角色位置、状态、关系、道具持有、技能可用性追踪。

### 知识图谱服务

负责实体和关系的复杂图查询。

核心实体：

- 作品。
- 章节。
- 场景。
- 事件。
- 角色。
- 地点。
- 道具。
- 技能。
- 能力。
- 阵营。
- 关系。
- 状态。

### 历法与时间域服务

负责用户可配置的时间系统。

必须区分：

- 世界内时间。
- 叙事展示顺序。
- 时间域。
- 时间流速映射。
- 架空历法显示格式。

### 版本快照服务

负责项目级或作品级大版本回滚。

优先级：

1. 项目状态快照。
2. 作品内容回滚。
3. 设定恢复。
4. 更细粒度的实体历史查看。

### 导出服务

首期只负责 Markdown 导出。

## 本地数据层

数据层采用多数据库组合，但必须由应用托管，用户不应感知后台服务或外部依赖。

建议分区：

- 作品内容库：章节、正文片段、大纲、项目配置。
- 图数据库或图索引：实体、关系、事件链、状态依赖。
- 向量索引：语义检索。
- 检查结果索引：冲突定位、严重级别、修复建议状态。
- 项目快照：版本回滚。

候选技术：

- `SQLite`：项目元数据、内容、快照、检查结果、FTS。
- 嵌入式图数据库或图服务：复杂图查询。
- 嵌入式向量库：语义检索。

Open Question: 图数据库最终选型需要技术 spike。

## AI Provider Adapter

首期只实现 OpenAI API。

接口必须抽象为 Provider Adapter：

- `generateText`
- `rewriteText`
- `extractCandidateFacts`
- `generateRepairPatch`
- `embedText`

约束：

- API key 不进入前端明文状态。
- 所有请求必须经过最小化上下文裁剪。
- 模型响应不可直接成为正式事实。

## 主要数据流

### 写作流

1. 用户在作品控制台选择章节。
2. 用户编辑正文或触发 AI 生成。
3. 正文保存到作品内容库。
4. 用户手动绑定事件，或 AI 抽取候选事件。
5. 用户确认候选事件后，写入正式事件和图谱。
6. 状态机更新受影响状态快照。
7. 增量检查更新检查结果。

### 冲突检查流

1. 用户手动触发检查或系统根据变更发起增量检查。
2. 检查服务计算影响范围。
3. 状态机和图谱服务执行约束检查。
4. 检查结果按 `error`、`warning`、`notice` 分级。
5. 前端在章节、事件、实体和正文位置标红。
6. 用户可请求生成修复补丁。
7. 用户按类型授权后应用补丁。

### Markdown 导出流

1. 用户选择导出范围。
2. 导出服务读取章节和正文。
3. 根据章节顺序生成 Markdown。
4. 不导出内部数据库实现细节。

## 技术风险

- 嵌入式图数据库在 Windows 打包中的体积和稳定性。
- 向量库与 Rust/Tauri 的集成成本。
- 项目级快照回滚的存储成本。
- 复杂时间域规则的 UI 可理解性。
- AI 抽取候选事实的误报和漏报。
