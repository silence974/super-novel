# PRODUCT_SENSE.md

## 目的

定义 `super-novel` 的产品判断基准，帮助后续 AI 和工程实现做一致取舍。

## 当前阶段

项目处于初始化与架构设计阶段。当前目标是划定总体框架、功能结构、数据模型、Agent 编排和文档约束，而不是立即实现全部功能。

## 目标用户

- 个人小说作者。
- 主要使用 Windows 桌面端。
- 以单个本地作品工程为创作单位。
- 希望 AI 辅助写作，但不希望 AI 自动接管创作决策。

## 核心问题

长篇小说创作中常见问题包括：

- 世界观和设定持续漂移。
- 角色在时间、空间上的位置不一致。
- 道具、技能、伤势、关系等状态缺少可追踪依据。
- AI 生成正文可能破坏既有事实。
- 大纲、事件、章节和正文之间缺少明确绑定。
- 作者修改正文后，冲突检查无法只针对受影响范围增量执行。

## 产品承诺

`super-novel` 不只是文本生成工具，而是一个可追踪、可校验、可修复的小说创作系统。

系统必须帮助作者回答：

- 某个时间点某角色在哪里。
- 某个角色当时持有哪些道具。
- 某两个角色当时是什么关系。
- 某个技能、能力、伤势、状态是否仍然有效。
- 某段正文是否违反已确认设定。
- 某个冲突可以用哪些补丁修复。

## 体验原则

- 人工掌控优先：AI 只提供候选，不自动写入正式事实库。
- 不阻断创作：强约束问题标红和说明，但不强制禁止保存。
- 可定位：每个冲突必须指向相关章节、事件、实体和规则。
- 可修复：系统应提供可预览的一键修复补丁。
- 可回滚：重要写作状态和设定变更必须能恢复。
- 本地优先：作品数据默认保存在本地数据库中。
- 用户无感部署：数据库、索引和服务应由应用托管，不要求用户手动安装外部依赖。

## 主要功能主线

- 作品控制台和分栏工作台。
- 世界观和设定库。
- 强约束时空状态机。
- 知识图谱和复杂图查询。
- 语义检索和向量索引。
- 大纲、分章大纲、故事流程和正文辅助生成。
- 润色、重写、冲突检查、设定修复。
- 手动检查和增量检查。
- 项目快照与版本回滚。
- Markdown 导出。

## 非目标

- 不做多人实时协作。
- 不做云端同步平台。
- 不做在线发布平台。
- 不做企业级权限、审计和容灾。
- 不要求完全离线 AI 推理。
- 首期不要求 PDF 或 EPUB 导出。
- 不把正文文本直接当作唯一事实源。

## 成功指标

- 作者能在一个作品控制台中看到章节、设定、事件、检查结果和 AI 操作入口。
- 用户可以配置架空历法和不同时间域。
- 系统可以在事件级和时间戳级追踪角色、地点、道具、关系和状态。
- AI 抽取的候选事实必须经过确认后才能入库。
- 冲突检查结果按 `error`、`warning`、`notice` 分级并标红定位。
- 用户可以对不同类型的修复补丁分别授权。
- Markdown 导出不依赖内部数据库结构。

## Assumptions

- 首期用户是单人作者，不需要任何多人协作设计。
- Markdown 是导出格式，不是主存储格式。
- AI Provider 首期只实现 OpenAI API，但接口需要预留扩展。
