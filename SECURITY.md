# SECURITY.md

## 目的

定义本地数据安全、API key 管理、AI 请求边界、日志脱敏和导出安全约束。

## 安全边界

产品是单人本地桌面应用，不提供多人权限模型。

当前适用：

- 本地数据库保护。
- API key 安全存储。
- OpenAI 请求上下文最小化。
- 日志脱敏。
- 导出边界控制。

当前不适用：

- 多租户隔离。
- 企业级权限审批。
- 团队审计。
- 云端数据同步安全。

未来如果引入同步、多人协作或在线发布，上述能力才变为适用。

## API key 管理

- API key 由用户配置。
- API key 不得写入项目导出内容。
- API key 不得出现在前端明文状态、日志或错误上报中。
- 推荐使用 Windows Credential Manager 或等价安全存储。
- 后端通过 Provider Adapter 读取密钥并发起请求。

## AI 请求边界

发送给 OpenAI API 的内容必须最小化。

原则：

- 只发送完成当前任务所需的章节、设定和事件上下文。
- 不默认发送整个作品数据库。
- 请求前向用户说明任务类型和大致上下文范围。
- 响应默认视为候选内容。

## 日志规则

日志不得包含：

- API key。
- 完整正文大段内容。
- 用户未确认的敏感设定。
- 原始模型请求全文。

允许记录：

- 请求类型。
- 模型供应商。
- token 或耗时统计。
- 错误码。
- 脱敏后的上下文范围。

## 导出安全

Markdown 导出只包含用户选择的作品正文和必要标题结构。

默认不导出：

- API key。
- 内部检查结果。
- 向量索引。
- 数据库 schema。
- 未确认候选事实。

## Provider Adapter 安全要求

- Provider 实现不得绕过统一日志策略。
- Provider 实现不得直接写入正式事实库。
- Provider 响应必须经过解析、标记和用户确认。
