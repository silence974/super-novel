# FRONTEND.md

## 目的

定义 Windows 桌面端的前端信息架构、分栏工作台、检查结果展示和人工授权交互。

## 设计目标

- Windows 原生体验优先。
- 安装体积可控。
- 主界面以作品控制台为中心。
- 支持长篇小说的章节、设定、事件和正文并行处理。
- 强约束问题必须可见、可定位、可解释。
- AI 操作必须人工触发。

## 信息架构

### 作品控制台

必须展示：

- 当前作品名称。
- 章节列表。
- 最近编辑章节。
- 当前冲突数量和严重级别。
- 最近快照。
- 常用 AI 操作入口。
- Markdown 导出入口。

### 分栏工作台

建议布局：

```text
左栏：作品结构与章节树
中栏：正文/大纲/场景编辑区
右栏：设定、事件、检查结果、AI 操作上下文
底部：状态栏、检查进度、快照状态
```

## 章节树

职责：

- 展示卷、章节、场景。
- 标记章节检查状态。
- 标记存在 `error`、`warning`、`notice` 的章节。
- 支持按章节触发增量检查。

## 正文编辑区

职责：

- 编辑章节正文。
- 显示冲突标红。
- 显示候选事件抽取结果。
- 支持用户手动将正文片段绑定到事件。
- 支持触发润色、重写、续写。

Open Question: 首期正文编辑器采用 Markdown 风格、富文本风格还是混合模式，需原型验证。

## 右侧上下文栏

可切换面板：

- 设定面板。
- 事件面板。
- 角色状态面板。
- 检查结果面板。
- AI 操作面板。
- 修复补丁面板。

## 冲突显示规则

- `error`：红色高亮，必须在章节树和正文定位中显示。
- `warning`：橙色高亮，显示原因和建议。
- `notice`：蓝灰色提示，不打断编辑。

每条检查结果必须显示：

- 严重级别。
- 问题描述。
- 涉及实体。
- 涉及章节或事件。
- 触发规则。
- 可用修复动作。

## AI 操作面板

所有 AI 操作必须由用户点击触发。

首期操作：

- 生成大纲。
- 生成分章大纲。
- 生成正文。
- 润色。
- 重写。
- 抽取候选事实。
- 生成检查说明。
- 生成修复补丁。

## 授权交互

一键修复必须按类型授权：

- 正文修改授权。
- 章节大纲修改授权。
- 事件修改授权。
- 状态修改授权。
- 关系修改授权。
- 世界观设定修改授权。

用户应能只应用部分补丁。

## 导出交互

首期只需要 Markdown 导出。

导出时应支持：

- 全作品导出。
- 指定章节导出。
- 按章节拆分导出。

## 不适用内容

多人在线协作界面当前不适用，因为产品定位为单人本地使用。未来如果引入云同步或团队协作，才需要补充协作、权限、评论和冲突合并界面。
