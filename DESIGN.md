# DESIGN.md

## 目的

定义小说编写助手的核心领域模型、状态约束、候选事实确认、冲突检查和修复补丁机制。

## 领域核心原则

- 正式事实必须来自用户确认。
- 正文不是唯一事实源。
- 事件是状态变化的主要依据。
- 状态机基于世界内时间推导，不基于叙事顺序推导。
- 倒叙只改变正文展示顺序，不改变事件真实发生顺序。
- AI 输出默认是候选材料。
- 所有修复都应先生成补丁，再由用户授权应用。

## 核心实体

### Project

一个本地作品工程，包含作品内容、设定库、图谱、索引、检查结果和快照。

### Work

具体作品，可包含多个卷、章节和场景。

### Chapter

章节是正文组织单位，也是导出 Markdown 的主要单位。

### Scene

场景是章节内更细的叙事单元，可绑定一个或多个事件。

### Entity

统一实体抽象，包含角色、地点、道具、技能、能力、组织、阵营等。

### Event

事件是状态变化的依据。事件必须包含世界内时间、时间域、参与实体、状态变化和来源。

### Fact

事实分为候选事实和正式事实。

- Candidate Fact：AI 抽取或用户草稿生成，尚未确认。
- Confirmed Fact：用户确认后进入正式事实库。

### State

某实体在特定时间点或时间区间内的状态，例如位置、健康、持有物、关系、技能可用性。

### Relation

实体之间的关系，例如角色关系、持有关系、所属关系、敌对关系、知识知晓关系。

### Repair Patch

修复补丁是对正文、设定、事件、状态或关系的可预览变更集合。

## 事件生命周期

```text
draft -> candidate -> confirmed -> superseded/deprecated
```

- `draft`：人工草稿，未进入检查主链。
- `candidate`：AI 或人工生成的候选事件。
- `confirmed`：用户确认，进入正式状态推导。
- `superseded`：被新事件替代。
- `deprecated`：被废弃但保留历史。

## 时间模型

必须同时记录：

- `world_time`：故事世界中的真实发生时间。
- `narrative_order`：正文展示顺序。
- `time_domain`：时间域，例如现实世界、秘境、梦境、回忆。
- `calendar_id`：架空历法配置。
- `time_scale_rule`：时间流速映射规则。

当前 spike 已验证单层整数比例时间域映射：异速时间域本地 tick 可以通过锚点和比例规则映射到 canonical `world_time`，同时保留独立的 `narrative_order`。

## 状态迁移

状态迁移由确认事件触发。

示例：

- 角色进入地点：更新角色位置。
- 道具交付：更新持有者。
- 技能封印：更新技能可用状态。
- 关系破裂：更新人物关系边。
- 受伤：更新健康状态。

## 冲突类型

### error

必须显著标红，表示强约束冲突。

示例：

- 同一角色在同一世界时间出现在两个互斥地点。
- 同一道具在同一时间被两个角色持有。
- 已死亡角色参与后续非回忆事件。

### warning

表示设定可能不完整或因果链不充分。

示例：

- 道具来源缺失。
- 角色突然掌握技能但缺少学习事件。
- 关系变化缺少过渡事件。

### notice

表示可改善的信息提示。

示例：

- 某个新设定未绑定章节来源。
- 某个状态长期未更新。
- 章节正文未绑定事件。

## 修复补丁模型

修复补丁必须包含：

- 目标类型：正文、章节大纲、事件、状态、关系、世界观设定。
- 修改前内容。
- 修改后内容。
- 影响范围。
- 风险说明。
- 是否需要二次检查。

应用规则：

- 不允许静默应用补丁。
- 必须按类型分别授权。
- 应用后必须触发增量检查。
- 应用后必须记录版本历史。

## Open Questions

- 图数据库最终选型未定。
- 项目级快照是否覆盖全部数据库文件仍需验证。
- 时间域映射的 UI 表达需要进一步原型化。
